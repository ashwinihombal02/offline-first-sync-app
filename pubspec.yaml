name: offline_first_sync_queue
description: Offline-first Sync Queue Assignment
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^3.4.0
  cloud_firestore: ^5.4.1

  hive: ^2.2.3
  hive_flutter: ^1.1.0

  connectivity_plus: ^6.0.5

  uuid: ^4.4.0
  intl: ^0.19.0

  flutter_riverpod: ^2.5.1

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true